use std::path::Path;

// Get the file from s3 and write into the desired location
pub fn fetch_file_from_s3(s3_path: String, local_write_path: &Path) -> Result<(), String> {
    // Get the config for the aws sdk
    // Read the file
}))

// Write file into the desired s3 location from the desired location

/*
use aws_config::meta::region::RegionProviderChain;
use aws_sdk_s3::{config::Region, Client};
*/
